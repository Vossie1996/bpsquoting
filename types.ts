
export interface PacdoraDimensionResults {
  flatLength: number;
  flatWidth: number;
}

export interface UserInput {
  pacdoraLink: string;
  length: number;
  width: number;
  height: number;
}
// Add other shared types here
